
# 用于解析slx文件所有模块的连线和block的参数设置
[上传slx文件]
解析这个slx，我要的是【完整 block 参数表 + 连接表】，只需要给我Input Processing模块内部的（包括所有内部的子模块）
注意：
1. 只需要解析这个slx模型中名为“Input Processing”的子系统：ROS2_simulink_UE5_0821/Subsystem/Vessel Platform/Input Processing/ 
2. 将参数表和连接表导出成表格CSV形式，让其他AI通过这个表格就可以复现出slx的连接和参数设置，从而知道公式和对应的构造机理

推荐AI：ChatGPT Agent，联网模式。 速度上优于GPT5.2，质量相当，速度和质量上都超过不联网的GPT deepresearch。
