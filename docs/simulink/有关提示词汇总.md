
# 用于解析slx文件所有模块的连线和block的参数设置
[上传slx文件]
解析这个slx，我要的是【完整 block 参数表 + 连接表】，只需要给我Input Processing模块内部的（包括所有内部的子模块）
注意：
1. 只需要解析这个slx模型中名为“Input Processing”的子系统：ROS2_simulink_UE5_0821/Subsystem/Vessel Platform/Input Processing/ 
2. 将参数表和连接表导出成表格CSV形式，让其他AI通过这个表格就可以复现出slx的连接和参数设置，从而知道公式和对应的构造机理

推荐AI：ChatGPT Agent，联网模式。 速度上优于GPT5.2，质量相当，速度和质量上都超过不联网的GPT deepresearch。


# 用于维护文档

读取仓库中docs/simulink/subsystem_748/vessel_platform/input_processing/input_processing_connections.csv 和
docs/simulink/subsystem_748/vessel_platform/input_processing/input_processing_parameters.csv，
它们呈现了simulink模型中subsystem_748/vessel_platform/input_processing这个模块下的各个block的连接关系和参数设置，两表的介绍和使用方法可以参考：Overview_input_processing.md。

然后，你查看仓库中docs/simulink/subsystem_748/vessel_platform/input_processing的各个.md文件（一共14个.md文件，一个文件对应一个子模块），核查以下几个方面：

1. 输入输出是否和csv中说的不符，如果情况存在，则修改至与csv文件相符

2. 计算公式是否和csv中说的不符，如果情况存在，则修改至与csv文件相符

3. 是否没有提及csv中提到的中间变量，如果情况存在，则需要添加

4. 计算公式中各的物理量与csv文件中的对应变量名字是否没有建立起对应关系，如果情况存在，则需要建立起对应关系，可以以表格的形式

修正以后，逐条汇报给我修正情况

注意：如果任务执行过程中出现：

File content (25458 tokens) exceeds maximum allowed tokens (25000). Please use offset and limit parameters to read specific portions of the file, or use the GrepTool to search for specific content.

你可以分段来进行，也可以选择使用GrepTool来检索。

推荐AI： CLaude code Opus 4.5. 目前所有与文档维护有关的都找它
